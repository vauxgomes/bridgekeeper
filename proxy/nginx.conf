worker_processes auto;

events {
    worker_connections 1024;
}

http {
    log_format json_access escape=json
        '{'
            '"time":"$time_iso8601",'
            '"remote_addr":"$remote_addr",'
            '"method":"$request_method",'
            '"uri":"$uri",'
            '"args":"$args",'
            '"status":$status,'
            '"body_bytes_sent":$body_bytes_sent,'
            '"request_time":$request_time,'
            '"http_user_agent":"$http_user_agent",'
            '"http_x_user_id":"$http_x_user_id",'
            '"http_x_forwarded_for":"$http_x_forwarded_for",'
            '"upstream_addr":"$upstream_addr",'
            '"upstream_response_time":"$upstream_response_time"'
        '}';

    access_log /var/log/nginx/access.log json_access;
    error_log  /var/log/nginx/error.log warn;

    # Rate limiting para /orders/ por IP
    limit_req_zone $binary_remote_addr zone=orders:10m rate=10r/m;
    limit_req_status 429;

    upstream api {
        server api:3000;
    }

    server {
        listen 80;
        server_name _;

        location /orders/ {
            # Aplica limite por IP, fazendo o NGINX retornar 429 quando a taxa for ultrapassada
            limit_req zone=orders burst=5 nodelay;

            proxy_pass         http://api;
            proxy_set_header   Host              $host;
            proxy_set_header   X-Real-IP         $remote_addr;
            proxy_set_header   X-Forwarded-For   $proxy_add_x_forwarded_for;
            proxy_set_header   X-User-Id         $http_x_user_id;
        }

        location /health {
            proxy_pass http://api;
        }

        location / {
            return 404 '{"error":"rota nao encontrada"}';
            add_header Content-Type application/json;
        }
    }
}
